services:
  - type: web
    name: tableau-to-powerbi-web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web/server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.6
